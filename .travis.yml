language: 'go'

services:
  - 'docker'

sudo: 'required'

go:
  - '1.9.2'

before_install:
  - './.travis/main.sh'

install:
  - 'sudo bash ./utils/main.sh'
  - 'make plugin'

script:
  - 'cd ./lib && sudo /home/travis/.gimme/versions/go/bin/go test -v'

notifications:
  email: false

