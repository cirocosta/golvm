language: 'go'

services:
  - 'docker'

sudo: 'required'

go:
  - '1.9.2'

before_install:
  - './.travis/main.sh'

install:
  - 'sudo bash ./utils/create.sh'
  - 'make plugin'

script:
  - 'make test'

notifications:
  email: false

